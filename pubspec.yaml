name: business_card_creator
description: A Business Card Creator app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_form_builder: ^6.1.0+1
  image_picker: ^0.8.4+2
  flex_color_picker: ^2.1.2
  mvc_pattern: ^7.4.0
  google_fonts: ^2.1.0
  flutter_masked_text2: ^0.9.1
  share: ^2.0.4
  widget_to_image: ^2.0.0
  path_provider: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.4

flutter:
  uses-material-design: true